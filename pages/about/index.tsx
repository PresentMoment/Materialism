import React from 'react'
import styled from "styled-components"
import Layout from '../../Components/Layout';

function about({data = {}}) {

  return (
    <Layout>
      <span>I started blah blah</span>
    </Layout>
  )
}

export default about;